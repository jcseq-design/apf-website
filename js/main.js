const GOOGLE_SHEETS_ENDPOINT = "";
document.addEventListener("DOMContentLoaded",()=>{document.querySelectorAll("#membership-form").forEach(form=>{form.addEventListener("submit",async e=>{e.preventDefault();const status=form.querySelector("#form-status");const data=Object.fromEntries(new FormData(form).entries());if(!GOOGLE_SHEETS_ENDPOINT){if(status){status.textContent="Formulário em preparação. O endpoint do Google Sheets ainda não foi configurado.";status.hidden=false}console.log("Dados preparados para integração:",data);return}try{await fetch(GOOGLE_SHEETS_ENDPOINT,{method:"POST",mode:"no-cors",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});form.reset();if(status){status.textContent="Obrigado. O seu pedido foi enviado.";status.hidden=false}}catch(err){if(status){status.textContent="Não foi possível enviar o formulário. Tente novamente.";status.hidden=false}console.error(err)}})})});